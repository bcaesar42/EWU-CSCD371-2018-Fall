<Window x:Class="TimeTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeTracker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="MyMainGrid" Margin="10" ColumnSpacing="5">

        <Grid.Resources>
            <Style TargetType="{x:Type ColumnDefinition}">
                <Setter Property=""
            </Style>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="StartAndStopPanel" Grid.Column="0" Grid.Row="0" Background="Green">
            
        </StackPanel>

        <ListBox x:Name="ActiveTasksBox" Grid.Column="1" Grid.Row="0" Background="Yellow">
            
        </ListBox>

        <ListBox x:Name="FinishedTasksBox" Grid.Column="2" Grid.Row="0" Background="Red">

        </ListBox>

        <Button x:Name="DeleteActiveTaskButton" Grid.Column="1" Grid.Row="1">
            
        </Button>
    </Grid>
</Window>
